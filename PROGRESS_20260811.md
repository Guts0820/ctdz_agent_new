# 项目进展报告 — 2026年8月10-11日

## 一、工作概览

两天完成了三大板块：**团队代码合并**、**前后端全链路打通**、**前端 MockData 清零**。后端正向链路和 Review 2.0 模块均可通过 HTTP 正常调用，前端 90% 以上数据来自真实数据库和知识图谱。

---

## 二、Day 1（8月10日）— 合并 + 接口打通

### 2.1 合并组员 OCR 模块

将组员仓库 `Guts0820/ctdz_agent_backend` 的 OCR 能力合并到项目中：

| 合并内容 | 来源 | 目标位置 |
|----------|------|----------|
| 手写 OCR 服务（PaddleOCR VL + 通义千问降级） | 组员 | `handwriting_ocr_service/` :8089 |
| 统一配置管理 `config.py` | 组员 | `backend/config.py`（加 Review 端口 8087、OCR 端口 8089） |
| 真实 OCR 判题（替换模拟版本） | 组员 | `backend/services/analysis_service.py` |
| 日志工具 `observability.py` | 组员 | `backend/services/observability.py` |
| 缓存工具 `cache_utils.py` | 组员 | `backend/services/cache_utils.py` |
| OCR 数据透传到前端 | 组员 | `api_gateway.py` submit 响应新增 `ocr` 字段 |
| LLM 工具函数 `call_llm_json` / `llm_enabled` | 自补 | `backend/services/llm_client.py` |

### 2.2 Review 2.0 模块接入网关

- 新建 `backend/services/review_service.py`（:8087），独立托管 Review 2.0 全部 API
- 网关添加 5 条代理路由：`/api/review-plans`, `/api/review-sessions`, `/api/attempts`, `/api/priority-runs`
- `start_all.py` 加入 Review Service（:8087）和 OCR Service（:8089），总计 10 个微服务

### 2.3 P0-3 对接真实答题数据源

- `Neo4jRepository._get_states_from_answer_history()` — 从 SQLite `answer_history` 读取答题记录
- 两路查 `knowledge_id`：先查 SQLite `question_knowledge_mapping`，再查 Neo4j `EXAMINES` 关系
- 聚合计算 `correct_count / wrong_count / correct_streak / wrong_streak`
- 无数据时降级到 Neo4j 查询（兼容旧链路）

### 2.4 P1-6 错因严重度映射

全链路：三级错因标签 → 数值化 `error_severity` → Neo4j `ANSWERED_QUESTION.error_severity` → `priority_calculator` 读取。

具体改动：
- `LEVEL1_SEVERITY` 映射表：概念→90，审题→60，计算→50，粗心→30
- `_calculate_error_severity()` — 取置信度最高的 tag，归一化为 0.0-1.0
- `apply_attempt_evidence()` — 用 MERGE 写入 Neo4j
- **三层降级不捏造**：LLM 主路径 → error_bank 关键词匹配 → 匹配不到返回空
- `match_error_tags()` / `match_error_tags_light()` — 改为查 error_bank 真实数据匹配
- `map_knowledge()` — 改为优先查 SQLite QKM 映射，降级关键词匹配 KG 知识点列表
- Error Analysis Agent 低置信度不再 400 拒绝，改为返回 `low_confidence: true` 标记

### 2.5 P1-7 订正流程改造 + P2-9 答案揭示逻辑

- `correct()` 已支持开放题（调 Analysis Service + Error Analysis Agent）
- 选择题 `judge_method` 修正为 `"local"`
- 答案揭示从"订正还错就揭示"改为"老师批次放行才揭示"
- `_is_answer_released()` 查询 `homework_batch` 表，支持 locked/partial/released 三种状态
- Review 题目的订正流程同步接入批次放行检查

### 2.6 Neo4j Student 节点属性统一

- 数据迁移：`MATCH (s:Student) SET s.id = s.student_id REMOVE s.student_id`（53个节点）
- 代码同步：`routers/students.py` 和 `neo4j_contracts.py` 中所有 `s.student_id` 改为 `s.id`

### 2.7 前端初始化

- API 网关添加 CORS 中间件
- 网关托管前端静态文件（`app_v2/`），访问 `http://127.0.0.1:8000/` 即前端
- `API_BASE` 改为相对路径 `/api`，消除跨域
- OCR Service 添加 CORS
- `.bat` 乱码修复 + 自动打开浏览器
- `PADDLEOCR_VL_DEVICE=cpu` 固化到 `.env`
- 安装 PaddleOCR、PaddlePaddle、python-multipart 等依赖

---

## 三、Day 2（8月11日）— MockData 清零 + 前后端联调

### 3.1 MockData 清零清单

| # | 位置 | 原 MockData | 替换方案 | 数据链路 |
|---|------|------------|----------|----------|
| S1 | 学生首页四格统计 | `MockData.studentStats` | `/api/student/{id}/stats` | answer_history SQLite 聚合 |
| S2 | 首页错题数文字 | `MockData.studentStats.totalMistakes` | 移除，改为文字提示 | — |
| S3 | 错题本列表 | `MockData.mistakes` | `/api/student/{id}/wrong-answers` | answer_history JOIN question |
| S4 | 成长报告-薄弱知识点 | `MockData.weakKnowledge` | `/api/students/{id}/weak` | Neo4j MASTERY < threshold |
| S5 | 成长报告-已掌握 | `MockData.masteredKnowledge` | `/api/students/{id}/mastery` + 过滤 ≥80% | Neo4j MASTERY |
| S6 | 成长报告-五维雷达 | `MockData.fiveDimensionScores` | 关键词映射到五维 + 掌握度取均值 | Neo4j MASTERY |
| T1 | 教师-错因分析图表 | `MockData.errorAnalysis` | `/api/class/{name}/mistake-stats` | answer_history SQLite 聚合 |
| T2 | 教师-高频错题TOP5 | `MockData.highFrequencyMistakes` | 同上接口 | 同上 |
| T3 | 教师-掌握度折线图 | `MockData.getTeacherDashboard` | `currentClassMastery` 真实数据 | Neo4j MASTERY |

### 3.2 新增后端接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/student/{id}/stats` | GET | 学生首页统计（总题数/正确率/错题数/已复习） |
| `/api/student/{id}/wrong-answers` | GET | 学生错题列表（来自 answer_history） |
| `/api/class/{name}/mistake-stats` | GET | 班级高频错题 TOP5 聚合 |

### 3.3 判题优化

- `_fuzzy_match()` — 字符串 → 两位小数比对 → 5%容差数值比对，解决"1÷7=0.14"被判错问题
- Analysis Service 返回 unknown 时自动降级为字符串/数值比对
- `question_selector` 选题加入 `random.shuffle` 打乱，避免每轮选相同题目
- 过滤纯图片题（无文字 prompt）和无效提示词

### 3.4 前端功能完善

- 拍照/相册：`takePhoto()` / `selectImage()` 改为真实调用摄像头/文件选择器 → OCR Service
- OCR 结果弹窗美化：置信度颜色标记、图片预览、提取算式、可折叠原始结果
- 学习路径：列表改为从 `/api/knowledge_points` 按年级加载，薄弱标记来自真实 API
- "开始学习"：弹窗显示知识点讲解/常见错误/教学要点（来自 Neo4j）
- "去复习这个知识点"：导航到首页 → 弹复习计划窗口
- 首页"今日推荐"：从 `/students/{id}/weak` 和 `/knowledge_points` 动态加载
- 教师"查看详情"：弹窗展示知识点内容 + 关联题目（查 Neo4j 和 KG API）
- 答题框：加格式提示"多个答案用中文逗号隔开"
- 订正按钮：session 完成后才显示，不提前暴露
- 底部间距：所有页面 `pb-4` → `pb-24`，不再被 Tab 遮挡

### 3.5 Bug 修复记录

- 网关代理超时 30s → 60s
- 网关代理 `POST /api/review-plans`（无路径尾缀）405 → 补 root 路由
- Review Service 创建计划/会话去除查重逻辑（每次新建）
- `renderHome` 异步代码在 `return` 后（死代码）→ 提取为独立方法
- 数组使用 `d.knowledgeMastery.map` 报错 → 直接 `d.map`
- 多处 `this` 绑定丢失 → 改用 `StudentPage.xxx()` / `TeacherPage.xxx()`

---

## 四、当前架构

```
前端 (app_v2) ← 网关托管 → http://127.0.0.1:8000/

API Gateway (:8000)
├── /api/v1/submit ────→ Analysis(:8081) → ErrorAnalysis(:8082)
│                              → Knowledge(:8083) → Teaching(:8084) → State(:8085)
├── /api/v1/teacher/* ──→ 批次管理（内置）
├── /api/students/* ────→ Neo4j 学生查询
├── /api/questions/* ───→ Neo4j 题目查询 + 向量搜索
├── /api/knowledge_points → Neo4j 知识点查询
├── /api/review-plans/* ─→ Review Service(:8087)
├── /api/review-sessions/* → Review Service(:8087)
├── /api/attempts/* ────→ Review Service(:8087)
└── /api/priority-runs/* → Review Service(:8087)

10 个微服务:
:8000 Gateway | :8007 KG Service | :8081 Analysis | :8082 ErrorAnalysis
:8083 Knowledge | :8084 Teaching | :8085 State | :8086 ReviewScheduler
:8087 Review | :8089 OCR

数据层:
├── SQLite (example_db.db): 18+ 表
├── SQLite (user_data.db): 用户系统
└── Neo4j: 知识图谱 (2905 节点)
```

---

## 五、下一步

### 5.1 全流程测试

按 `TEST_CHECKLIST.md` 逐项验证：
- 教师端：创建批次 → 一键放行/精细放行 → 查看掌握度/错题分析
- 学生端：拍照录入 → 复习答题 → 订正错题 → 查看错题本/成长报告/学习路径

### 5.2 已知待修

| 优先级 | 问题 | 说明 |
|--------|------|------|
| P1 | 题库质量 | 部分题目答案错误（如"十万位答案是6"），需人工筛或加过滤 |
| P1 | 登录系统 | MockData.currentUser 仍然是模拟的，无真实登录 |
| P2 | 五维评分 | 学习韧性维度目前固定50，需串联 streak 数据 |
| P2 | OCR 置信度 | CPU 推理 ~30-50%，GPU 可提升但未配置 |

### 5.3 临时文件

测试/验证脚本（交付前可清理）：
`submit_test.py` `verify_changes.py` `migrate_student_id.py` `test_review_api.py`
`test_integration.py` `test_ocr.py` `test_ocr_real.py` `check_*.py` `debug_*.py`
`quick_test.py` `test_proxy.py` `test_correct.py`
