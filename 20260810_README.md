# AI 小学数学错题订正系统

## 环境要求

- Python 3.10+
- Neo4j（已启动，默认 bolt://localhost:7687）
- Windows / macOS / Linux

## 一、安装依赖

```bash
cd ct dz_Agent
pip install -r backend/requirements.txt
```

## 二、初始化数据库（首次运行）

```bash
python backend/database/init_db.py
```

## 三、启动后端（一键启动全部 9 个服务）

```bash
python backend/start_all.py
```

启动后会有如下输出：

```
All services started!
============================================================
API Gateway:              http://localhost:8000
Analysis Service:         http://localhost:8081
Error Analysis Agent:     http://localhost:8082
Knowledge Service:        http://localhost:8083
Teaching Service:         http://localhost:8084
State Service:            http://localhost:8085
Review Scheduler:         http://localhost:8086
Review Service:           http://localhost:8087
Knowledge Graph Service:  http://localhost:8007
============================================================
```

按 `Ctrl+C` 停止全部服务。

## 四、启动前端

双击 `app_v2/启动Demo.bat`，浏览器会自动打开 `http://localhost:3000`。

或手动：
```bash
cd app_v2
python -m http.server 3000
# 浏览器访问 http://localhost:3000
```

## 五、登录账号

前端默认提供三个角色：

| 角色 | 账号 | 说明 |
|------|------|------|
| 教师 | 任意教师账号 | 仪表盘、班级管理、**作业批次创建与答案放行**、错题分析 |
| 学生 | 任意学生账号 | 答题、查看讲解、复习计划 |
| 管理员 | 任意管理员账号 | 系统统计、数据管理 |

## 六、核心功能验证

### 6.1 学生提交作业（完整链路）

```bash
curl -X POST http://127.0.0.1:8000/api/v1/submit \
  -H "Content-Type: application/json" \
  -d '{"student_id":"S-0001","question_id":"Q-0001","student_write":"25+38=63"}'
```

链路：判题 → 错因分析 → 知识点查询 → 讲解生成 → 状态更新 → 复习计划

### 6.2 老师创建作业批次 + 答案放行

打开前端 → 教师角色 → 底部 Tab 点击"作业" → "创建作业批次" → 勾选题目 → 确认

批次默认 **锁定（locked）**，学生看不到完整答案。
点击 **"一键放行"** 或 **"精细放行"** 后，答案对学生可见。

### 6.3 学生复习（Review 2.0）

前端 → 学生角色 → 底部 Tab 点击"复习" → 创建复习计划 → 开始答题 → 订正

## 七、服务架构

```
前端 (app_v2, :3000)
  │
  ▼
API Gateway (:8000)  ← 统一入口
  ├── /api/v1/submit          → 判题+错因+讲解+状态 (5个微服务协作)
  ├── /api/v1/teacher/*       → 批次管理+答案放行
  ├── /api/students/*         → Neo4j 学生查询
  ├── /api/questions/*        → Neo4j 题目查询
  ├── /api/review-plans/*     → Review 2.0 (代理→:8087)
  ├── /api/review-sessions/*  → Review 2.0 (代理→:8087)
  ├── /api/attempts/*         → Review 2.0 (代理→:8087)
  └── /api/priority-runs/*    → Review 2.0 (代理→:8087)
```

## 八、详细文档

| 文档 | 说明 |
|------|------|
| `CODE_READING_GUIDE.md` | 代码阅读指南（架构+模块清单） |
| `FINAL_BACKEND_FILES.md` | 后端文件清单（最终版本） |
| `PROJECT_PROGRESS.md` | 项目进展报告 |
| `backend/PROJECT_OVERVIEW.md` | 后端详细架构文档 |
| `app_v2/README.md` | 前端技术栈说明 |

## 九、常见问题

**Q: 启动报 `ModuleNotFoundError`？**
```bash
pip install -r backend/requirements.txt
```

**Q: Neo4j 连接失败？**
确认 Neo4j 已启动，检查 `backend/.env` 中的连接信息。

**Q: 前端 API 请求失败？**
确认后端 `python backend/start_all.py` 已启动，网关在 :8000 端口。

**Q: 修改代码后没生效？**
`Ctrl+C` 停掉 `start_all.py`，重新运行。或单独重启对应服务。
